# FASTA File Parser

This project uses Biopython to parse a FASTA file and extract information from DNA sequences.

## What the program does

For each DNA sequence, the program extracts:

- Sequence ID
- Sequence length
- GC content

## Requirements

- Python 3
- Biopython

Install Biopython using:

```bash
pip install biopython
